export enum OrdersStatus {
    PENDING = "pending",
    PAID = "paid",
    SHIPPED = "shipped",
    DELIVERED = "delivered",
    CANCELED = "canceled",
    REFUNDED = "refunded",
    COMPLETED = "completed",
    FAILED = "failed"
}

